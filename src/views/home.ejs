<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <link rel="stylesheet" href="/CSS/style.css">
    <style>
        .table-container {
            padding: 15px 20px;
        }

        .title {
            font-size: 25px;
            font-weight: 600;
            padding-bottom: 10px;
        }

        table {
            border-collapse: collapse;
            width: 100%;
        }

        th,
        td {
            padding: 8px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        tr:hover {
            background-color: #ccc;
        }
    </style>
</head>

<body>
    <div class="topnav">
        <a class="active" href="/">Home</a>
        <a href="/create">Creater User</a>

    </div>
    <div class="table-container">

        <div class="title">List Users</div>
        <table class="w3-table">
            <tr>
                <th>ID</th>
                <th>Email</th>
                <th>Name</th>
                <th>City</th>
                <th>Actions</th>
            </tr>

            <!-- <% for (let i=0; i< listusers.length; i++ ) {%>
                <tr>
                    <td>
                        <%= listusers[i].id %>
                    </td>
                    <td>
                        <%= listusers[i].email %>
                    </td>
                    <td>
                        <%=listusers[i].name %>
                    </td>
                    <td>
                        <%=listusers[i].city %>
                    </td>
                    <td>
                        <button>Edit</button>
                        <button>Delete</button>
                    </td>
                </tr>
                <%} %> -->
            <% listusers.forEach(function(user) { %>

                <tr>
                    <td>
                        <%= user.id %>
                    </td>
                    <td>
                        <%= user.email %>
                    </td>
                    <td>
                        <%=user.name %>
                    </td>
                    <td>
                        <%=user.city %>
                    </td>
                    <td>
                        <a href="/update/<%= user.id %>">Edit</a>
                        <a href="/deleteuser/<%= user.id %>"
                            onclick="return confirm('bạn có chắc muốn xóa user này không/')">delete</a>
                    </td>
                </tr>
                <% }); %>
        </table>
    </div>


</body>

</html>